class LightDirectional {
    ambient;
    diffuseColor;
    direction;

    constructor(ambient, diffuseColor, direction) {
        this.ambient = ambient;
        this.diffuseColor = diffuseColor;
        this.direction = direction;
    }
}